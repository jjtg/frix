{
  "name": "frix-example",
  "version": "1.0.0",
  "description": "Example project demonstrating frix library features",
  "type": "module",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "migrate": "kysely migrate:latest --config kysely.config.ts",
    "migrate:down": "kysely migrate:down --config kysely.config.ts"
  },
  "dependencies": {
    "frix": "file:..",
    "kysely": "^0.27.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "kysely-ctl": "^0.8.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
